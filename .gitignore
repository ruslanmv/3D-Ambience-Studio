# Python build and cache artefacts. Absent until now, which is how a first run of
# `pytest` put 12 .pyc files into a commit.
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
build/
dist/

# Runtime data: generated projects, optimised work files and published output.
data/
